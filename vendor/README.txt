This folder holds every pip package EbaratNeshan needs so conversion runs with no internet.

- wheels/  pip-downloaded .whl files (install source)
- libs/    unpacked packages used at runtime by run.py and python -m mdmaker

Python itself must still be installed. The converter never calls the network.
