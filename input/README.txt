Put PDF or DOCX files here and set "input": "" in config.json to convert all of them.

EbaratNeshan (عبارت‌نشان) writes Markdown for LLMs and for reading.
Persian and Arabic Word PDFs with broken fonts are the main case; English and other LTR files work too.
